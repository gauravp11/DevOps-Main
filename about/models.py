# No models are needed as per your requirement
# models.py

# No models are needed as per your requirement

# models.py
from django.db import models

class about(models.Model):
    pass

    class Meta:
        verbose_name = "about"
        verbose_name_plural = "about"  # Singular and plural are the same to avoid pluralization